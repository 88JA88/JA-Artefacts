name: Mettre à jour les données pour VITRINE

on:
  push:
    branches: [main]
    paths:
      - CATALOGUE/donnees.js
      - ANALYSES/*.pdf

permissions:
  contents: write

jobs:
  publier:
    runs-on: ubuntu-latest
    steps:
      - name: Lire le catalogue
        uses: actions/checkout@v4

      - name: Préparer Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Installer les outils d’image
        run: python -m pip install --disable-pip-version-check pypdf Pillow

      - name: Générer les titres et vignettes
        run: python CATALOGUE/generer_echange_vitrine.py

      - name: Publier les fichiers mis à jour
        run: |
          if git diff --quiet -- CATALOGUE/vitrine.json CATALOGUE/vignettes; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CATALOGUE/vitrine.json CATALOGUE/vignettes
          git commit -m "Mettre à jour les données pour VITRINE"
          git push
